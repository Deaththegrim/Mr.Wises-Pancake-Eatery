/* META — identity constants.
   Nothing else anywhere hardcodes a title, so renaming the game is this
   one line. Set it to null and the UI falls back to "Pancake Shop". */

export const META = {
  title: "Mr. Wise's Pancake Eatery",
  saveVersion: 1,
  saveKey: 'pancake-shop-save'
};
